$(document).ready(function () {
    $("#lightSlider").lightSlider({
            gallery: true,
            minSlide: 1,
            maxSlide: 1,
            currentPagerPosition: 'left'
        }
    );
});